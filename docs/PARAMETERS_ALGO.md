# All options above the modem

Generated by `docs/make_parameters.py` from the code itself — do not edit by hand.
The C++ modem's own ~100 options are in [`PARAMETERS.md`](PARAMETERS.md); anything not
named below is still reachable with `--usrp-set KEY=VALUE` / `--lora-set KEY=VALUE`.

## `./run.sh` — 46 options

| Flag | Meaning | Default |
|---|---|---|
| `--algo` | folder name under algorithms/ |  |
| `--role` | loopback \| chain \| gossip \| multi \| aircomp \| tx \| rx \| relay \| peer, or any role the algorithm declares in ROLES (e.g. client / server). Case-insensitive. Defaults to loopback, or to peer when --node is given. |  |
| `--node` | run ONE node of a decentralised network as this process: which node am I (0-based). Implies --role peer; --agents says how many nodes there are and --topology which of them I exchange with. |  |
| `--peers` | comma-separated host per node, indexed by node id (default: all 127.0.0.1, i.e. several terminals on this machine) | `` |
| `--peer-port` | base TCP port for peer exchange; node k listens on peer-port + k | `5800` |
| `--peer-link` | how decentralised peers exchange: tcp (LAN / same machine), wireless (the USRP radio), lora (the LoRa radio). Defaults to the PHY --channel names — lora for --channel lora, else tcp. — one of `tcp`, `wireless`, `lora` |  |
| `--channel` | WHICH PHY carries the payloads: ideal (radio-free, lossless), usrp (drivers/usrp), lora (drivers/lora). HOW that PHY is attached is the matching --<phy>-backend flag. Aliases: sim=ideal, pyphy=usrp. — one of `ideal`, `sim`, `usrp`, `pyphy`, `lora` | `ideal` |
| `--usrp-backend` | how the USRP PHY is attached: pyphy (the repo's real C++ modem in-process, no radio) or radio (real USRPs — needs the two-host role split, --role tx / --role rx) — one of `pyphy`, `radio` | `pyphy` |
| `--steps` |  | `5` |
| `--agents` | number of agents (--role multi) | `4` |
| `--relays` | number of relay nodes between the two ends (--role chain) | `1` |
| `--topology` | peer graph for --role gossip: ring (default) \| full \| an explicit edge list such as 0-1,1-2,2-0 (any graph, e.g. a line or a star) | `ring` |
| `--freq` | centre frequency in MHz (default 915). Both PHYs use it. The US ISM band is 902-928 MHz and a value outside it is flagged. | `915.0` |
| `--tx-gain` | USRP transmit gain, dB | `70.0` |
| `--rx-gain` | USRP receive gain, dB | `30.0` |
| `--samp-rate` | USRP sample rate in Hz (default 2e6) | `2000000.0` |
| `--ack-transport` | USRP ACK path: tcp (a socket — no reverse RF needed) or rf (a second RF path, RF B, which needs the reverse link and a full-duplex box). The DATA always goes over RF either way. — one of `tcp`, `rf` | `tcp` |
| `--ack-timeout` | USRP: how long the source waits for an ACK before resending (default 3000 ms). LoRa derives its own from the airtime of a frame, which scales correctly across SF7..SF12. | `3000` |
| `--arq` | retransmission policy. Only stop-and-wait is implemented so far (the C++ ACQ_stop_and_wait.hpp, and the LoRa driver's framing); naming it means a run records the policy it used, and a new scheme is an entry in ARQ_SCHEMES rather than an edit everywhere. A PHY that lacks the one you ask for says so. — one of `stop-and-wait` | `stop-and-wait` |
| `--max-attempts` | stop-and-wait: give up on a chunk after this many un-ACKed sends. Applies to whichever PHY is selected; the default is each PHY's own (USRP 50, LoRa 8 — LoRa retries are expensive because each one costs a full frame of airtime). |  |
| `--symbol-rate` | USRP symbol rate in Hz (default 1e6; sample rate / this is the oversampling factor the receiver expects) | `1000000.0` |
| `--lora-backend` | how the SX1276 is attached: sim (no hardware), serial (Arduino/Teensy on USB), spi (Pi with the radio on its SPI bus) — one of `sim`, `serial`, `spi` | `sim` |
| `--lora-sf` | spreading factor: higher reaches further and costs exponentially more airtime — one of `7`, `8`, `9`, `10`, `11`, `12` | `9` |
| `--lora-cr` | coding rate denominator (4/5 .. 4/8) — one of `5`, `6`, `7`, `8` | `5` |
| `--lora-bw` | bandwidth in Hz. The SX1276 supports more, but these are the ones used in US 902-928 operation: 125 kHz (the normal channels), 250 kHz, and 500 kHz (the wide channel). Doubling the bandwidth halves the airtime and costs ~3 dB of sensitivity. — one of `125000`, `250000`, `500000` | `125000` |
| `--lora-power` | TX power in dBm | `14` |
| `--lora-port` | serial backend: the port the radio is on, e.g. /dev/ttyUSB0 |  |
| `--lora-verbose` | print fragments / retransmissions / airtime for every message |  |
| `--usrp-set` | any other USRP modem variable, by its own name — repeatable. e.g. --usrp-set det_mult=5 --usrp-set viz=true. Full list: docs/PARAMETERS.md or sdr_system --help | `[]` |
| `--lora-set` | any other LoRa driver variable, by its own name — repeatable. e.g. --lora-set seed=7 --lora-set reply_timeout=60 | `[]` |
| `--waveform` | USRP waveform: single-carrier, or OFDM (CFO-robust — worth trying on a marginal link). Both ends must agree. — one of `sc`, `ofdm` | `sc` |
| `--tx-subdev` | USRP transmit RF channel (B210: A:A = RF A, A:B = RF B) | `A:A` |
| `--rx-subdev` | USRP receive RF channel (N210/X310: A:0) | `A:0` |
| `--tx-ant` | USRP transmit connector (default TX/RX) | `TX/RX` |
| `--rx-ant` | USRP receive connector (default RX2) | `RX2` |
| `--scheme`, `--modulation` | USRP modulation (BPSK/QPSK/8-PSK/16-QAM/DBPSK/DQPSK). --modulation is the same flag. | `QPSK` |
| `--fec` | USRP forward error correction. LoRa has its coding rate (--lora-cr) and its CRC in the chip instead. — one of ``, `conv`, `ldpc`, `turbo` | `turbo` |
| `--sim-snr-db`, `--snr-db` | SIMULATION ONLY (--snr-db is the older spelling): the link SNR the simulated channels model — --usrp-backend pyphy adds AWGN at this Es/N0, and --lora-backend sim tests it against the spreading factor's demodulator floor. On REAL hardware SNR is MEASURED, not set: drive the link with --tx-gain/--rx-gain (USRP) or --lora-power/--lora-sf (LoRa) and read the SNR the receiver reports back. | `8.0` |
| `--radio` | THIS node's USRP: serial=30CD424 (B210) or addr=192.168.40.2 (X310/N210); a bare serial or IP also works. Sets both --tx-args and --rx-args, so one flag names the radio this process owns. Give --tx-args/--rx-args instead when a node has two radios. | `` |
| `--tx-args` | UHD device args of the transmit radio | `` |
| `--rx-args` | UHD device args of the receive radio | `` |
| `--ack-host` |  | `127.0.0.1` |
| `--net-host` |  | `127.0.0.1` |
| `--net-port` | TCP reply port this node SERVES to the node upstream of it | `5700` |
| `--down-host` | relay only: host of the next hop downstream |  |
| `--down-port` | relay only: that node's --net-port (default: --net-port + 1) |  |

## LoRa driver variables — 13

Set any of these with `--lora-set NAME=VALUE`. The named `--lora-*` flags above cover the
common ones; this is the complete surface, taken from `LoRaChannel.__init__`.

| Variable | Default |
|---|---|
| `backend` | `sim` |
| `sf` | `9` |
| `cr` | `5` |
| `bw_hz` | `125000` |
| `power_dbm` | `14` |
| `freq_hz` | `915000000` |
| `snr_db` | `0.0` |
| `seed` | `0` |
| `max_attempts` | `8` |
| `port` | `None` |
| `freq_mhz` | `None` |
| `verbose` | `False` |
| `arq` | `stop-and-wait` |
